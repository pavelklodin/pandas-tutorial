- [Описание архитектуры задачи B1](#описание-архитектуры-задачи-b1)
  - [Хранение исходных данных](#хранение-исходных-данных)
    - [Immutable исходные данные](#immutable-исходные-данные)
    - [Метаданные клиентов](#метаданные-клиентов)
    - [Загруженные новые данные для обработки](#загруженные-новые-данные-для-обработки)
    - [Обновления метаданных клиентов](#обновления-метаданных-клиентов)
  - [Хранение промежуточных результатов](#хранение-промежуточных-результатов)
  - [Хранение итоговой аналитики](#хранение-итоговой-аналитики)
  - [Устойчивость алгоритма к сбоям](#устойчивость-алгоритма-к-сбоям)
    - [State management](#state-management)
    - [Реакция на сбои и прерывание _analytic pipeline_](#реакция-на-сбои-и-прерывание-analytic-pipeline)
  - [Обработка данных](#обработка-данных)
    - [Режим запуска скрипта](#режим-запуска-скрипта)
    - [Механизм определения «последней версии» заказа (order deduplication)](#механизм-определения-последней-версии-заказа-order-deduplication)
      - [`ingestion_date`](#ingestion_date)
      - [index](#index)
      - [Order deduplication invariant](#order-deduplication-invariant)
    - [Методика расчета нетривиальных метрик (строки `ALL` в итоговой аналитике)](#методика-расчета-нетривиальных-метрик-строки-all-в-итоговой-аналитике)
      - [Колонки и их логика](#колонки-и-их-логика)
      - [Январь 2024 (тестовые данные)](#январь-2024-тестовые-данные)
      - [Февраль 2024 (тестовые данные)](#февраль-2024-тестовые-данные)
      - [Март 2024 (тестовые данные)](#март-2024-тестовые-данные)
    - [Алгоритм работы _analytic pipeline_](#алгоритм-работы-analytic-pipeline)
    - [Post-conditions](#post-conditions)

# Описание архитектуры задачи B1

## Хранение исходных данных 

### Immutable исходные данные 

Эти данные **никогда не перезаписываются и не изменяются**: каждая дата папки соответствует фактическому ingestion-batch'у.

Хранятся в папке 
```
data/input/<year>/<month>/<day> 
```
Пример имени папки 
```
data/input/2025/10/25 
```
Она содержит файлы с информацией о продажах, поступившие в систему _25 октября 2025 года_, обработанные и попавшие в итоговую аналитику (в файл `data/result/revenue_summary.csv`). 

### Метаданные клиентов 

Хранятся в файле 
```
data/input/customers.csv 
```
### Загруженные новые данные для обработки 

Хранятся в папке 
```
/data/input/to_process 
```
Формат имени файла 
```
orders-<year>-<month>-<day>-000x.csv 
```
Где `x` - порядковый номер файла (если таких файлов за день поступило несколько, начинается с нуля). Будем считать, что больше _1000_ файлов за день нам не поступит - это гарантия, звучит разумно. 

Пример имени файла 
```
orders-2025-10-25-0000.csv 
```
Это первый по очереди файл с информацией о продажах, поступивший в систему _25 октября 2025 года_. 

Дата указывает на поступление данных в систему. Данные могут содержать как информацию о продажах за прошлые даты (_late arrivals_), так и информацию, уточняющую ранее внесенные в систему продажи. 

### Обновления метаданных клиентов 

Хранятся в файле 
```
data/input/to_process/customers.csv 
```
## Хранение промежуточных результатов 

Хранятся в файле 
```
data/current_intermediate/enriched_raw_data.csv 
```
Это файл с очищенными и сджойненными данными (заказы и клиенты), обогащенный метриками: 
- `revenue`;
- `order_month`.

**ВАЖНО!** Файл `enriched_raw_data.csv` является **source of truth** для инкрементальной обработки и расчета итоговой аналитики.

## Хранение итоговой аналитики 

Хранится в файле 
```
data/result/revenue_summary.csv 
```
Это файл `enriched_raw_data.csv`, сгруппированый по:
- `order_month`;
- `segment` внутри `order_month`.

и обогащенный метриками:
- `cumulative_revenue`;
- `rolling_avg_revenue`. 

для каждого сегмента и строками с total'ами по каждому месяцу. Строки вставляются после группы сегментов месяца. 

## Устойчивость алгоритма к сбоям 

### State management 

Состояние - это:
```
(обработанные файлы) + (enriched_raw_data.csv)
```
State Management осуществляется:
- Раздельным хранением уже обработанных и еще не обработанных файлов; 
- Перемещением необработанных файлов к обработанным после завершения _analytic pipeline_. 

### Реакция на сбои и прерывание _analytic pipeline_ 

- В этом случае недообработанные файлы остаются в исходной папке. При следующем запуске скрипт их обработает. 
- Если какие-либо из данных уже ранее обрабатывалсь, то это не приведет к сбоям в работе, поскольку они буду считаны, как дублированные и отброшены при очистке исходных данных, до совершения всех вычислений. 

## Обработка данных

### Режим запуска скрипта

Считаем, что **скрипт запускается раз в сутки**, и для простоты больше файлов с новыми данными в эти сутки не будет. Это очень легко организовать, **запуская скрипт по cron'у в 23:50**, например.

### Механизм определения «последней версии» заказа (order deduplication)

Инкрементальный пайплайн должен корректно обрабатывать поздние обновления заказов (_late-arriving data_) и ситуации, когда один и тот же `order_id` встречается в нескольких входных файлах в разные дни.

Чтобы гарантировать детерминированный выбор «самой новой» записи для каждого заказа, используется двухуровневый механизм сортировки:

- `ingestion_date` — извлекается из имени файла, определяет момент поступления данных в систему;
- `index` — порядковый номер строки внутри файла, используемый как детерминированный _tie-breaker_.

После сортировки по этим двум полям выполняется `drop_duplicates(order_id, keep="last")`, что обеспечивает выбор последней версии заказа.

Ниже описаны детали.

#### `ingestion_date`

`ingestion_date` — это техническое поле, позволяющее определить порядок поступления версии заказа в систему.

**Как формируется**

Значение извлекается из имени входного файла вида:

`orders-YYYY-MM-DD-NNNN.csv`

и формируется как строка:

`YYYY-MM-DD-NNNN`

где:

- `YYYY-MM-DD` — дата поступления файла,
- `NNNN` — четырёхзначный sequence number внутри дня.

Пример:

`orders-2025-10-25-0003.csv → ingestion_date = "2025-10-25-0003"`

**Зачем нужно**

Это поле обеспечивает:

1. Глобальный порядок поступления данных.
   
   «Поздняя» запись о заказе, введённая в систему позже, получает больший ingestion_date.
2. Корректную обработку late-arriving data.
   
   Позднее пришедшая корректировка заказа будет иметь более поздний ingestion_date и заменит предыдущую версию записи.
3. Детерминированность результата.
   
   Независимо от порядка чтения файлов, данные всегда будут отсортированы одним и тем же образом.

**Где используется**

В функции:

```
orders_df = orders_df.sort_values(by=["order_id", "ingestion_date"])
orders_df = orders_df.drop_duplicates(subset=["order_id"], keep="last")
```

То есть `ingestion_date` фактически является частью _ordering key_ для выбора последней версии заказа.

#### index

`index` — вспомогательное поле (создаваемое `reset_index()` при чтении файла), которое используется как стабильный _tie-breaker_ внутри одного файла.

Поле `index` **не попадает** в промежуточный результат и **не сохраняется**.

**Когда возникает необходимость**

Если в одном и том же входном файле встречаются несколько строк с одинаковым `order_id` (редкий, но возможный случай):

- `ingestion_date` у них одинаковый
- и тогда сортировка только по `ingestion_date` недостаточна для выбора последней строки.

**Как формируется**

При чтении файла выполняется:

```
new_orders_df = new_orders_df.reset_index()
```

`Pandas` создаёт колонку `index`, которая:

- содержит порядковый номер строки из исходного CSV,
- стабильно растёт сверху вниз.

**Где используется**

В функции `cleanup_data()` модуля [`data_utils.py`](task-b01/data_utils.py):

```
cleaned_df = cleaned_df.sort_values(
    by=["ingestion_date", "index"],
    ascending=[True, True]
)
cleaned_df = cleaned_df.drop_duplicates(subset=["order_id"], keep="last")
```

То есть:

- `ingestion_date` сортирует _«между файлами»_
- `index` сортирует _«внутри файла»_

**Назначение**

`index` нужен только как _детерминированный tie-breaker_.

Он не нужен вне одного батча и не должен сохраняться.

#### Order deduplication invariant

Термин **invariant** означает _неизменяемое правило_, которое должно выполняться всегда при любой обработке данных в пайплайне.

**Инвариант пайплайна B1:**

```
Последняя версия записи заказа — это строка с максимальной парой (ingestion_date, index).
Следовательно, drop_duplicates(keep="last") после сортировки по этим полям всегда выбирает корректную актуальную версию заказа.
```

**Почему это критически важно**

1. **Устранение дублей между разными ingestion-датами.**
   
   Поздняя корректировка всегда побеждает.

2. **Детерминированность при повторах внутри одного файла.**
   
   Последняя строка в файле заменяет предыдущие.

3. **Идемпотентность запуска.**

   Повторный запуск даст тот же результат:
   ordering key одинаков → результат dedup одинаков.

4. **Безопасность при сбоях.**
   
   Если файлы обработаны частично, но ingestion_date сохраняется, после перезапуска результат будет корректен.

### Методика расчета нетривиальных метрик (строки `ALL` в итоговой аналитике)

Это строки, которые содержат сводные данные по всем сегментам, поэтому в колонке `segment` имеют значение `ALL`.

#### Колонки и их логика
- **Total Revenue (месяца)**: Сумма выручки всех сегментов за текущий месяц.
- **Cumulative Revenue (ALL)**: Накопительный итог всей выручки (total_revenue) с самого начала (января) до текущего месяца.
- **Rolling Avg Revenue (ALL)**: Среднее значение total_revenue за последние 3 месяца (текущий + 2 предыдущих).

#### Январь 2024 (тестовые данные)
- **Выручка по сегментам**: Enterprise (500) + SMB (200) = 700.
- **Cumulative**: Только первый месяц, поэтому просто 700.
- **Rolling Avg**: Так как данных раньше нет, это $\frac{700}{1} = 700$.

#### Февраль 2024 (тестовые данные)
- **Выручка по сегментам**: Enterprise (600) + UNKNOWN (1000) = 1600.
- **Cumulative**: Январь (700) + Февраль (1600) = 2300.
- **Rolling Avg**: Среднее за январь и февраль: $\frac{700 + 1600}{2} = 1150$.

#### Март 2024 (тестовые данные)
- **Выручка по сегментам**: Только SMB (300).
- **Cumulative**: Предыдущий итог (2300) + Март (300) = 2600.
- **Rolling Avg**: Среднее за январь, февраль и март: $\frac{700 + 1600 + 300}{3} = \frac{2600}{3} \approx 866.67$.

### Алгоритм работы _analytic pipeline_

1. **Проверяем pre-conditions**:

   0. Общая **цель** проверки - обеспечить **штатный запуск** скрипта. В случае невозможности штатно запустить скрипт выдается соответствуюшее сообщение об ошибке. В случае потенциально опасной ситуации выдается предупреждение о необходимости более строгой проверки целостности данных.
   
      Проверяется общая целостность данных (наличие или отсутствие необходимых файлов).

      Соответствие исходных данных результату **не проверется**!
   1. Должны существовать **метаданные кастомеров**.
      - обработанные данные `data/input/customers.csv`

        **ИЛИ**
      - данные, которые еще нужно обработать `data/input/to_process/customers.csv`

      Если их **нет** - работа завершается с **ошибкой** (нет метаданных кастомеров).
   2. Должны существовать **данные по заказам для обработки**.

      Если **нет** `/data/input/to_process/orders-<year>-<month>-<day>-000x.csv` - работа **завершается** с сообщением, что нечего обрабатывать (**не ошибка!**).
   3. В заключении проверяется **целостность обработанных данных**.
      - Если **обработанные данные отсутствуют** (папки `data/input/<year>/<month>/<day>` - пусты) - значит это первый старт и промежуточного результата (файл `data/current_intermediate/enriched_raw_data.csv`) **не может существовать**. Поэтому:
        - Если файла `data/current_intermediate/enriched_raw_data.csv` **нет**, проверка завершена **успешно** (первый старт).
        - Если файл `data/current_intermediate/enriched_raw_data.csv` **есть**, работа продолжается и **логируется предупреждение**, что вероятно протеряна часть данных. Рекомендуется ревью и восстановление утеряных данных из backup'а.
      - Если **обработанные данные присутствуют** (папки `data/input/<year>/<month>/<day>` - не пустые) - значит это обычный (не первый) старт и промежуточный результат (файл `data/current_intermediate/enriched_raw_data.csv`) **обязан существовать**. Поэтому:
        - Если файл `data/current_intermediate/enriched_raw_data.csv` **есть**, проверка завершена **успешно** (обычный старт).
        - Если файла `data/current_intermediate/enriched_raw_data.csv` **нет**, работа **завершается** с ошибкой, что потерян промежуточный результат. Рекомендуется переместить обработанные данные в папку `/data/input/to_process/` и перезапустить скрипт для восстановления промежуточного результата.
2. **Считываем метаданные клиентов** из файла
   ```
   data/input/customers.csv 
   ```
   в `DataFrame` 
   ```
   customers_df 
   ```
   `DataFrame` с метаданными клиентов нам будет нужен в любом случае, для приведения формата новых данных к промежуточным результатам.
   Создадим его пустым в самом начале, так что `customers_df`, после обработки метаданных кастомеров, всегда будет содержать наиболее актуальные значения.
3. **Если есть обновленные метаданные клиентов** в файле
   ```
   data/input/to_process/customers.csv
   ```
   - Считываем обновленные метаданные клиентов из файла 
     ```
     data/input/to_process/customers.csv 
     ```
     в `DataFrame` 
     ```
     customers_to_process_df
     ```
   - Конкатенируем `customers_df` с `current_customers_df`
   - Если данные дублируются - оставляем последнюю запись, даже если раньше у клиента был сегмент, а теперь - не стало.
   - Сохраняем обновленные метаданные клиентов в
     ```
     data/input/customers.csv.tmp 
     ```
   - Заменяем временный файл на постоянный
     ```
     data/input/customers.csv
     ```
     с помощью
     ```
     os.replace(tmp, target) 
     ```
   - Удаляем
     ```
     data/input/to_process/customers.csv 
     ```
4. **Считываем загруженные новые данные** - все файлы из папки 
   ```
   /data/input/to_process 
   ```
   в `DataFrame`
   ```
   orders_to_process_df
   ```
   Если файлов с новыми данными несколько - читаем в цикле и конкатенируем.
   В этот момент мы точно знаем, что новые данные есть, иначе не прошли бы `pre_conditions()`.
5. **Обрабатываем и обогащаем** `orders_to_process_df` 
   - Джойним с `customers_df`
   - Очищаем данные 
   - Обогащаем их метриками
     - `revenue`
     - `order_month` 
6. **Считываем промежуточные результаты** из файла 
   ```
   data/current_intermediate/enriched_raw_data.csv 
   ```
   в `DataFrame` 
   ```
   current_orders_df
   ```
7. **Обновляем данные о клиентах**, если нужно
   ```
   current_orders_df.update(customers_df)
   ```
8. **Конкатенируем** `current_orders_df` и `orders_to_process_df` в `concatenated_orders_df` 
   - **Очищаем** `concatenated_orders_df` в `cleaned_orders_df` и в случае одинаковых `order_id` - оставляем только последнюю запись.
   - **Обновляем** файл с промежуточными результатами
     - Сохраняем `cleaned_orders_df` в файл
       ```
       data/current_intermediate/enriched_raw_data.csv.tmp
       ```
     - Заменяем временный файл на постоянный
       ```
       data/current_intermediate/enriched_raw_data.csv
       ```
       с помощью
       ```
       os.replace(tmp, target)
       ```
9. **Перемещаем файлы с обработанными новыми данными** из папки
   ```
   /data/input/to_process
   ```
   в
   ```
   data/input/<year>/<month>/<day>
   ```
10. **Пересчитываем** `cleaned_orders_df` и `DataFrame` для итоговой аналитики - `final_result_df` 
11. **Сохраняем** `final_result_df` в файл
    ```
    data/result/revenue_summary.csv.tmp
    ```
    - Заменяем временный файл на постоянный 
      ```
      data/result/revenue_summary.csv
      ```
      с помощью
      ```
      os.replace(tmp, target)
      ```

### Post-conditions

1. В папке `to_process` нет необработанных файлов.
2. Файл `enriched_raw_data.csv` отражает _полный актуальный state_.
3. Файл `revenue_summary.csv` синхронизирован с `enriched_raw_data.csv`.